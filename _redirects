# Netlify Redirects for Single Page Application routing

# API proxy (when you add backend)
# /api/*  https://your-backend-api.com/:splat  200

# Card terminal - serve directly (not SPA routed)
/terminal  /card-terminal.html  200

# POS display - serve directly
/pos  /pos.html  200

# SPA routing - all other routes go to index.html
/*    /index.html   200
